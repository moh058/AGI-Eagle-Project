name: AGI-Telegram-Build
on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: System Setup
        run: |
          sudo apt update
          sudo apt install -y build-essential git openjdk-17-jdk python3-dev zip unzip libncurses5 libstdc++6
          pip install --upgrade pip
          pip install "cython<3.0.0" buildozer kivy requests

      - name: Hard-Reset Spec & Build
        run: |
          # حذف أي ملف قديم وبدء صفحة جديدة
          rm -f buildozer.spec
          buildozer init
          
          # استبدال شامل لا يقبل الجدل (إجبار المحرك على API 33)
          sed -i 's/^requirements = .*/requirements = python3,kivy,requests/' buildozer.spec
          sed -i 's/^android.api = .*/android.api = 33/' buildozer.spec
          sed -i 's/^android.minapi = .*/android.minapi = 24/' buildozer.spec
          sed -i 's/^# android.accept_sdk_license = False/android.accept_sdk_license = True/' buildozer.spec
          
          # أمر البناء الصارم مع تسجيل كل شاردة وواردة
          yes | buildozer -v android debug > full_build_log.txt 2>&1

      - name: Delivery or Detailed Intel
        if: always()
        run: |
          APK_PATH=$(find bin/ -name "*.apk" || echo "none")
          if [ "$APK_PATH" != "none" ]; then
            curl -F "chat_id=${{ secrets.CHAT_ID }}" -F "document=@$APK_PATH" -F "caption=🚀 النصر! التطبيق جاهز." https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendDocument
          else
            # في حال الفشل، سنرسل آخر 100 سطر بدلاً من 20 لنرى أين بدأت المشكلة فعلياً
            tail -n 100 full_build_log.txt > final_report.txt
            curl -F "chat_id=${{ secrets.CHAT_ID }}" -F "document=@final_report.txt" -F "caption=❌ فشل تقني. حلل هذا الملف يا جماني." https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendDocument
          fi
